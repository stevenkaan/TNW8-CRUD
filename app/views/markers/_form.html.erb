<%= form_for([@city, @city.markers.build]) do |f| %>
  <p>
  <%= f.label :marker_images %><br>
  <%= f.file_field :marker_images %>
  </p>
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>