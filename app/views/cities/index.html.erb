<h1>Listing Cities</h1><a href="cities/new">New city</a>

<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
  </tr>

  <% @cities.each do |city| %>
    <tr>
      <td><%= city.city_name %></td>
      <td><%= city.city_text %></td>
    </tr>
  <% end %>
</table>
